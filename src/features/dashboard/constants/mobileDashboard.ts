/**
 * Primary in-app surface for field QR workflows: in-dashboard camera scanner plus
 * `/qr/*` landing routes for decoded stickers.
 */
export const DASHBOARD_MOBILE_SCAN_ENTRY_PATH = '/dashboard/scan' as const;
