---
description: "Authoritative feature-branch PR workflow — open through merge (CI green, Supabase green or skipped, evidence). Same standard every time."
alwaysApply: true
---
# PR Merge-Ready Workflow (authoritative)

Every **product/runtime PR into `preview`** (day-to-day) follows this end-to-end standard. **Opening a PR is not handoff** — the agent owns the branch until the merge gate passes, then **merges**.

**Release PRs** (`preview` → `main`, or `chore/release-v*` from the curated preview tip) use the same babysit gates with `--base main`.

Applies when the user or an invoked publish skill asked to open or land a PR (`branching.mdc` publish mode). Does **not** apply because a linked worktree exists. Does **not** apply to local-iterate work on the current checkout.

Direct push to `main` and `preview` is blocked by branch protection; merge via PR after `local-verify-before-preview-push.mdc`.

## Merge gate (preview)

Merge when **all** of the following are true. Do **not** wait for Qodo (retired — pricing). Review threads are not a merge blocker unless the user asks to address them.

| Gate | Verify |
|------|--------|
| Local verify | Fallow clean, lint, type-check, targeted/`test:ci`, build when relevant, local E2E per change type |
| PR visual evidence | Screenshots + MP4 demo in PR body; `-Publish` comment when product UI changed; when help/docs discovery changed, include proof from each surfaced entry point to the guide |
| CI green | `gh pr checks <num> --watch` — every required job is success. Expected skips (for example Release Metadata on a `preview` base) are fine |
| Supabase | `Validate Supabase Migrations` and `Supabase Preview` are **success**, **or** skipped / absent because the PR has no related `supabase/**` (or validator) changes |
| Mergeable | `gh pr view <num> --json mergeable,mergeStateStatus` → `MERGEABLE` / `CLEAN` |

Then merge:

```powershell
gh pr merge <num> --merge
```

Do not pass `--delete-branch` while the PR head is checked out in this worktree (`git-powershell.mdc`).

Do **not** report "PR opened" or walk away while CI or Supabase is failing or pending.

## Phase 1 — Branch and implement

Only after publish mode is on (`branching.mdc`). If you are already on a matching `feat/*` or `fix/*` branch, keep it. Otherwise:

```powershell
git fetch origin preview
git switch -c feat/<short-slug> origin/preview
```

Implement with focused commits. Run Fallow before each commit per `fallow-before-commit.mdc`.

## Phase 2 — Local verification (before first push)

Run in order:

1. Fallow (both scans)
2. **`npm ci --prefer-offline --no-audit`** when lockfile/deps changed or before first PR open — on Windows, run `.\dev\dev-stop.bat` first if EPERM/EBUSY is possible (`dev-stack-lifecycle.mdc`). Linux CI is authoritative if local `npm ci` is blocked by file locks after a successful prior install.
3. `npm run lint`, `npm run type-check`, `npm run test:ci`, `npm run build`, `npm run verify:spa-routing` when applicable
4. Ensure stack healthy via batch files (`dev-stack-lifecycle.mdc`); use `.\dev\dev-start.bat -Force` when migrations/seeds affect E2E
5. Local E2E / PR evidence capture per `local-verify-before-preview-push.mdc` and `pr-visual-evidence.mdc`

## Phase 3 — Open PR

```powershell
git push -u origin HEAD
```

Write PR body to a UTF-8 file (never inline `gh pr create --body` on Windows — `git-powershell.mdc`). Include visual evidence markdown from `tmp/pr-evidence/<flow>/evidence-markdown.md`.

Day-to-day feature/fix PR:

```powershell
gh pr create --base preview --head feat/<short-slug> --title "<conventional title>" --body-file "$env:TEMP\pr-<slug>-body.md"
.\dev\pr-evidence\Invoke-PrEvidence.ps1 -Flow "<slug>" -Spec "e2e/pr-evidence/<feature>.spec.ts" -PrNumber <num> -Publish
```

Release / promote PR (`preview` → `main` or `chore/release-v*`):

```powershell
gh pr create --base main --head preview --title "Release vX.Y.Z" --body-file "$env:TEMP\pr-release-body.md"
```

## Phase 4 — Babysit until merge

Repeat until the merge gate passes, then merge:

### 4a — CI first

```powershell
gh pr checks <num> --watch
```

If red: fix on branch, Fallow + local verify, push, return to 4a. CI failures outrank review comments.

### 4b — Supabase

Confirm `Validate Supabase Migrations` and `Supabase Preview` are success, or skipped / not scheduled because this PR does not touch related Supabase paths. A red Supabase job blocks merge even when GitHub Actions CI is otherwise green.

### 4c — Merge

```powershell
.\dev\pr-feedback\Get-PrChecks.ps1 -PullRequestNumber <num> -Json
gh pr view <num> --json mergeable,mergeStateStatus,url
gh pr merge <num> --merge
```

Post a short merge comment citing the green CI run URL and the Supabase result (pass or skipped / not applicable).

## Fix-forward patterns (from production PRs)

- **Migration validator:** register RPCs in `dev/security-definer-rpc-allowlists.json`; add `-- rpc-authenticated-grant-allowed:` markers per function.
- **CodeQL:** fix findings on changed files; avoid dynamic `RegExp` construction from user/path input in E2E specs when a literal pattern suffices.
- **E2E fixture drift:** reset idempotent test org state (e.g. `e2e/user/shared/fresh-start-reset.ts`) before evidence capture; ensure seed cleanup allowlists include fixture orgs.

## Relationship to other rules

- **`pr-ci-gate-before-open.mdc`:** CI parity install + green checks — subset of this workflow.
- **`pr-visual-evidence.mdc`:** capture and publish requirements.
- **`address-pr-feedback/SKILL.md`:** triage loop when the user asks to address review comments — not a preview merge gate.
- **`branching.mdc`:** local-iterate vs publish; feat → preview → main. Never direct-push to `main` or `preview`.

## Stop conditions (escalate, do not abandon PR)

- Finding requires product decision outside scope — post question on PR, stop babysit loop.
- CI or Supabase failure unrelated to PR scope persists after merging latest `origin/preview` (feature) or `origin/main` (release) — report with failing job link.
- Merge conflict — merge/rebase the PR base (`origin/preview` or `origin/main`), re-verify, push, restart Phase 4.
