# EquipQR v3.28.0 — production post-promote migrations

Applied automatically by **Production Release Readiness** after merge of the `preview` → `main` promote. Enumerated here so the release PR carries an explicit checklist.

## Checklist

- [ ] `supabase/migrations/20260719214316_security_advisor_1310_hardening.sql` — Security Advisor hardening: drop public-bucket listing policies, pin DEFINER `search_path` / EXECUTE allowlist, revoke `PUBLIC`/`anon` from public functions, re-grant authenticated INVOKER surface
- [ ] `supabase/migrations/20260720002037_security_advisor_1310_anon_invoker_relock.sql` — Follow-up: re-assert three-token anon surface and deny-by-default for new RPCs

No manual SQL required when Production Release Readiness completes successfully.
