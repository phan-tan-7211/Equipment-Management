# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
test-output.txt

node_modules
node_modules.bak-*
node_modules_broken
dist
dist-ssr
*.local
.local*

# Editor directories and files
.vscode/*
.vs/*
!.vscode/extensions.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vs/VSWorkspaceState.json
tmp/*
# Pinned lint binaries provisioned by dev/lint-catalog.mjs
tmp/lint-tools/
tmpclaude-*-cwd
*superpowers*

# Environment files (secrets)
.env
.env.*
!.env.example
supabase/.env

# Private keys and credentials
*.pem
*.key
credentials.json
service-account*.json

# Generated volume seed data - regenerated by dev/seed-data/generate-seeds.ts
# on every dev-start -Force / E2E -ResetDb run. Never commit (issue #1164).
supabase/seeds/generated/

# Backup Files used for Testing
supabase/*.backup
supabase/.temp/*
supabase/.branches/*
supabase/snippets/Untitled*.sql

# Coverage Files
coverage/*

# Vitest JSON test-results (durations / slow-test capture — see #1349)
artifacts/

# Github Rulesets (for debugging)
github.ruleset*.json
deno.lock

# Playwright Files
.playwright-mcp/*
.playwright-cli/*
playwright-report/
test-results/

#Ignore vscode AI rules
.github\instructions\codacy.instructions.md

# Agent ecosystems not used in this Cursor-only workspace
.agents/
.claude/
skills-lock.json

# Cursor hook runtime state (generated during chat sessions)
.cursor/hooks/state/

# Cursor agent session plans (ephemeral)
.cursor/plans/

# Python bytecode from local hook runs
.cursor/hooks/__pycache__/

# Firecrawl scrape/research output — local-only agent artifacts.
# Kept on disk for reference but never committed.
.firecrawl/
.fallow/

.vercel

# VitePress build artifacts — regenerated on every docs dev/build run.
docs/.vitepress/cache/
docs/.vitepress/.temp/

# Build artifact — regenerated by dev/generate-sitemap.ts on every `npm run build`.
# Tracking it produces noisy PR diffs and breaks build reproducibility.
public/sitemap.xml

# Local-only dev helpers written by the demo-mobile-screenshot skill.
# These are mobile-emulation iframe wrappers used by the agent for screenshots
# and must never ship to production.
public/__mobile-frame.html
public/__viewport-probe.html

# Homepage collage source drop. Compose with `npm run collage:compose`.
# App stills live in public/images/landing/; do not ship raw sources.
public/images/marketing/

node_modules_broken/@tailwindcss/.oxide-win32-x64-msvc-EpFgz722.old/tailwindcss-oxide.win32-x64-msvc.node
